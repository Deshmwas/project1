export interface OrderViewModel {
 productId: number;
  userId: number;
  quantity?: number;
  totalPrice?: number;
  status?: string;
  shippingDate?: string;
  shippingAddress?: string;
  orderDate?: string;
}

